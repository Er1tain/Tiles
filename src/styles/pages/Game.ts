import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
`

export const SettingsButton = styled.button`
    width: max-content;
    margin-top: 50px;
`
export const RowTiles = styled.div`
    display: flex;
    flex-direction: row;
    gap: 10px;
`

export const ColsTiles = styled.div`
    display: flex;
    flex-direction: column;
    gap: 10px;
`